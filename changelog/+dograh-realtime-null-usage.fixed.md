- `OpenAIRealtimeLLMService`, and `AzureRealtimeLLMService` with it, closes a turn whose `response.done` carries `"usage": null`, as a cancelled response does: usage is reported only when present, and `stop_processing_metrics`, the `TTSStoppedFrame`, the `LLMFullResponseEndFrame` and the rest of the handler run either way. It used to raise before closing the turn, which also ended the receive loop. The receive loop now skips a server event it cannot parse, such as an event type the fork does not model, with a warning that names the event's type and the error's first line but never the raw event, instead of ending and leaving the session deaf. A fatal `error` event still ends the loop.
