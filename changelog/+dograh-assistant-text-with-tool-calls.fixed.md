- The Anthropic, AWS Bedrock, Gemini, Gemini Live and OpenAI Responses adapters now keep what the assistant said beside its tool calls. A tool-call message was converted from its calls alone, so the text never reached the model; it now comes first and the calls after it, in the same provider message, and missing, empty or whitespace-only text adds nothing, since Anthropic and Bedrock reject a blank text block. Gemini's parallel-tool merge still groups a signed call that carries text with its unsigned siblings.
