- `VADAnalyzer.cleanup()` shuts down the single-thread executor it runs inference on, and drops its audio buffer. The body was `pass`, and `ThreadPoolExecutor` has no finaliser, so a server that builds one analyzer per call parked one worker thread per call for the life of the process. The smart-turn analyzer's identical executor was already released this way; this was the half that was missed.
