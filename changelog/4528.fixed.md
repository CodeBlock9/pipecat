- `DograhSTTService` now reports STT usage on each final transcript instead of only flushing at stop/cancel, so a session that is cancelled rather than ended no longer loses the audio seconds it consumed. This matches `DeepgramSTTService` and the other streaming providers.
