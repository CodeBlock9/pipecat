- The per-context synthesis watchdog is now armed *after* `run_tts`'s generator returns, and only for the websocket shape it exists for — a generator that returned having yielded nothing. Armed before the request as it previously was, its countdown started marginally earlier than the deadline around the iteration, so an HTTP-shaped service that stalled reported the same sentence twice: once from the watchdog and once from the generator deadline. Two reports per stall reached `synthesis_timeout_burst` on the second stalled sentence rather than the third.
