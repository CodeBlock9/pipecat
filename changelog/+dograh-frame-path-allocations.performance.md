- The reached-frame filters on `PipelineWorker` are stored as tuples instead of sets, so the `isinstance` test on the source and sink push paths no longer builds a tuple for every frame in both directions; and `FastAPIWebsocketOutputTransport` forwards the audio frame it was handed instead of rebuilding it, keeping its subclass, timestamps and transport destination, and rebuilding only when the sample rate or channel count genuinely differ.
