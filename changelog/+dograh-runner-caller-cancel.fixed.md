- `WorkerRunner.run()` re-raises a cancel of the task awaiting it once it has torn its workers down, so an `asyncio.wait_for` or `asyncio.timeout` deadline around it raises `TimeoutError` and a cancelled parent ends cancelled. It swallowed that cancel and returned normally. The runner's own stop (`end()`, `cancel()`, a signal, the last root worker finishing) still returns normally.
