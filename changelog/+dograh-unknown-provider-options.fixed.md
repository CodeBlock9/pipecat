- Provider options that are not parameters of the provider SDK's method now ride `extra_body` instead of raising `TypeError` before the request is sent, on every turn. Options an OpenAI-compatible endpoint understands — DeepSeek's `thinking`, say — keep reaching it, and ones the provider rejects come back as a provider response the application can classify. The OpenAI chat-completions and Responses services and `AnthropicLLMService` each check against their own wire method, and warn once per service naming what they moved.
