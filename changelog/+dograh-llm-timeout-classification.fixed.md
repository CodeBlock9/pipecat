- A language-model request that reaches its deadline fires `on_completion_timeout` and reports `LLM completion timeout` for every shape the deadline can take — `httpx.TimeoutException` while the stream is iterated, `openai.APITimeoutError` from a client-level timeout on `create()`, and `asyncio.TimeoutError`. Only the first was recognised, so a generation that never started was reported as a generic completion error.
